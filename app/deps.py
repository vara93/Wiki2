from fastapi import Depends

# Placeholder for shared dependencies (auth, database, etc.)
# Currently unused but can be extended.

def get_current_user():
    # Stub for future authentication integration
    return None
